import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";

const layout = await readFile("src/app/layout.tsx", "utf8");
const globals = await readFile("src/app/globals.css", "utf8");
const game = await readFile("src/components/LiarlineGame.tsx", "utf8");
const ui = await readFile("src/components/ui.tsx", "utf8");
const notebook = await readFile("src/components/NotebookDrawer.tsx", "utf8");
const keyboardHook = await readFile("src/hooks/useKeyboardInset.ts", "utf8");

assert.ok(layout.includes("interactiveWidget"));
assert.ok(globals.includes("--keyboard-inset"));
assert.ok(globals.includes(".mobile-action-dock"));
assert.ok(keyboardHook.includes("visualViewport"));
assert.ok(game.includes("useKeyboardInset"));
assert.ok(game.includes("dockRef"));
assert.ok(game.includes("ResizeObserver"));
assert.ok(game.includes("setProperty(\"--interrogation-dock-height\""));
assert.ok(game.includes("transcriptStackRef"));
assert.ok(game.includes("dockHeight"));
assert.ok(game.includes("scrollLatestTurnAboveDock"));
assert.ok(game.includes("scrollBy"));
assert.ok(game.includes("coveredByDock"));
assert.ok(game.includes("mobile-action-dock"));
assert.ok(game.includes("paddingBottom: \"calc"));
assert.ok(game.includes("acknowledgedRisk"));
assert.ok(game.includes("degradedAiTitle"));
assert.ok(game.includes("visualThesisBadge"));
assert.ok(game.includes("start-interrogation-surface"));
assert.ok(game.includes("first-question-cta"));
assert.ok(game.includes("CaseProgressRail"));
assert.ok(game.includes("case-progress-rail"));
assert.ok(game.includes("case-progress-beat"));
assert.ok(game.includes("suspectSelectAria"));
assert.ok(game.includes("witness-status-strip"));
assert.ok(game.includes("witness-answer-badges"));
assert.ok(game.includes("role=\"status\""));
assert.ok(game.includes("role=\"alert\""));
assert.ok(game.includes("accusation-risk-screen"));
assert.ok(game.includes("selected-evidence-counter"));
assert.ok(game.includes("final-risk-warning"));
assert.ok(game.includes("risk-acknowledge-checkbox"));
assert.ok(game.includes("resolution-complete-screen"));
assert.ok(game.includes("missed-opportunity-card"));
assert.ok(notebook.includes("compact-evidence-surface"));
assert.ok(notebook.includes("role=\"dialog\""));
assert.ok(notebook.includes("suspicion-signal-board"));
assert.ok(notebook.includes("suspicion-signal-card"));
assert.ok(notebook.includes("grid grid-cols-3"));
assert.ok(!notebook.includes("{clue.clueId}</p>"));
assert.ok(notebook.includes("String(index + 1).padStart"));
assert.ok(ui.includes("noninteractive-suspect-card"));
assert.ok(ui.includes("suspectSelectAria"));
assert.ok(globals.includes(":focus-visible"));
assert.ok(globals.includes("overflow-wrap: anywhere"));
assert.ok(globals.includes("prefers-contrast"));
assert.ok(globals.includes(".transcript-turn"));
assert.ok(globals.includes("scroll-margin-bottom"));

console.log("mobile UI contract tests passed");
